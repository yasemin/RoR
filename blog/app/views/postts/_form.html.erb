<%= form_for(@postt) do |f| %>
  <% if @postt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@postt.errors.count, "error") %> prohibited this postt from being saved:</h2>

      <ul>
      <% @postt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
